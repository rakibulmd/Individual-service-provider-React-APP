import React from "react";
import Loading from "../Shared/Loading/Loading";

const About = () => {
    return (
        <div>
            <Loading></Loading>
        </div>
    );
};

export default About;
